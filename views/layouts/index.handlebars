<ul>
    {{#each Article}}
    <li>
        <a href="{{this.url}}">{{this.headline}}</a>
        <p>{{this.summary}}</p>
        <button data-id="{{this.id}}" class="saveArticle">Save Article</button>
    </li>
    {{/each}}
</ul>